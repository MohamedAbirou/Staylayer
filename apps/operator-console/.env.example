# Operator Console environment
# Base URL of the backend API. Leave empty to use the dev proxy (/api -> http://localhost:4000).
VITE_API_URL=

# Marketing app login URL used to redirect unauthenticated operators.
# In production this should point at the operator login page once introduced in Phase 2.
VITE_OPERATOR_LOGIN_URL=/operator/login
